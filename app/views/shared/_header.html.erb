<div class="nav">
  <div class="nav_menu">
    <%= link_to 'Test Guru', root_path %>
    <% unless logged_in? %>
      | <%= link_to 'Sign in', login_path unless current_page?(login_path) %>
      <%= link_to 'Sign up ', signup_path unless current_page?(signup_path) %>
    <% end %>
  </div>
  <div class="nav_user">
    <% if logged_in? %>
      Welcome <b><%= current_user.email %></b> Guru |
      <%= link_to 'Exit', logout_path, method: :delete %>
  <% end %>
  </div>
</div>