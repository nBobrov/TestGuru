<tr>
  <td><%= question.body %></td>
  <td><%= link_to t('helpers.shared.links.show'), admin_question_path(question) %></td>
  <td><%= link_to t('helpers.shared.links.edit'), edit_admin_question_path(question) %></td>
  <td>
    <%= link_to t('helpers.shared.links.delete'),
                admin_question_path(question),
                method: :delete,
                data: { confirm: t('.confirm_delete') } %>
  </td>
</tr>
