<h1>Editing Answer</h1>

<%= link_to 'Back', admin_question_path(@answer.question) %>

<p>
  <%= @answer.question.body %>>
</p>

<%= render 'form', answer: @answer %>

<%= link_to 'Show', [:admin, @answer] %>
