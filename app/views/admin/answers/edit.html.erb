<h1><%= t('.header') %></h1>

<%= link_to t('.back'), admin_question_path(@answer.question) %>

<p>
  <%= @answer.question.body %>>
</p>

<%= render 'form', answer: @answer %>

<%= link_to t('.show'), [:admin, @answer] %>
