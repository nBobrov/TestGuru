<div class="row justify-content-center m-3">
  <h1><%= t('.header') %></h1>

  <%= render 'shared/errors', resource: @test %>

  <table class="table table-striped">
    <tr>
      <th><%= t('.name') %></th>
      <th><%= t('.img') %></th>
      <th><%= t('.rule_type') %></th>
      <th><%= t('.rule_value') %></th>
      <th colspan="3"><%= t('.actions') %></th>
    </tr>
    <%= render @badges %>
  </table>

  <%= link_to t('helpers.shared.links.add_badge'), new_admin_badge_path(@badge) %>
</div>