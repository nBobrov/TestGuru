<div class="row justify-content-center m-3">

  <h1><%= t('.header') %></h1>

  <table class="table table-striped">
    <tr>
      <th><%= t('.body') %></th>
      <th><%= t('.url') %></th>
      <th><%= t('.user_email') %></th>
    </tr>
    <%= render @gists %>
  </table>
</div>